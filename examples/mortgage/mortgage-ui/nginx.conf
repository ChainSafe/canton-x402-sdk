server {
  listen 80;

  # Proxy API calls to the mortgage backend (compose service name + default port).
  location /api/ {
    proxy_pass http://mortgage-backend:4002/;
  }

  # Static SPA with history fallback.
  location / {
    root /usr/share/nginx/html;
    try_files $uri /index.html;
  }
}
