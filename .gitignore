# Dependencies
node_modules/

# Build output — a publish artifact now (built via prepack, shipped in the npm
# tarball via each package's `files`), never committed.
dist/
coverage/
*.tsbuildinfo

# Environment (contains party IDs, secrets, OAuth credentials)
.env
.env.local
.env.*.local
examples/**/.env

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Claude
.claude/

# Canton key files (never commit)
*-key.json
*.key.json
