/**
 * @chainsafe/x402-core
 *
 * Placeholder — the real wire types, canonical requirements hashing, verify
 * primitives, and network presets land in the following issues. This export
 * exists only to prove the toolchain (dual ESM + CJS + d.ts) end to end.
 */
export const VERSION = "0.0.0";
