---
"@chainsafe/x402-core": minor
---

Recompute the canonical Canton prepared-transaction hash during verification, reject signed hashes that do not match the submitted transaction, and expose verification as an asynchronous API.
